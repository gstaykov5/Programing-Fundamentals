function solve(name, count) {
    count = Number(count);
    let time = 3;
    let totalHour = count * time;
    console.log(`The architect ${name} will need ${totalHour} hours to complete ${count} project/s.`)

    
}